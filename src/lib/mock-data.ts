import type { DayEntry } from '@/lib/types';

// This data is now loaded from localStorage.
// This file is kept to avoid breaking imports, but is not actively used by the calendar.
export const mockData: DayEntry[] = [];
